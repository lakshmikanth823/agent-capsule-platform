# Architecture & Requirements Decisions Log

This document records all clarifications, architectural decisions, and requirement resolutions approved by the user, per the Project Rules.

| Date (YYYY-MM-DD) | Topic / Question                             | Resolution / Decision                                                                                                                                                                                                                                                                                                             | Reference Documents                                                                                         |
| :---------------- | :------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------- |
| 2026-09-21        | Project Setup & Rules                        | Formalized project rules, security invariants, review protocols, and source of truth hierarchy.                                                                                                                                                                                                                                   | `docs/TRD.md`, `docs/PRD.md`, `.agents/rules/project-rules.md`                                              |
| 2026-09-21        | Resource Naming & API Paths                  | Standardize on `/v1/capsules` in the public REST API and `capsule_id` (or `id`) as the identifier to maintain consistency with `docs/TRD.md` (Source of Truth #1) and the core product concept. Database models represent `Capsule`, `CapsuleVersion`, and `CapsuleShare`.                                                        | `docs/TRD.md` Sec 4, `docs/api-cli-spec/API_CLI_SPEC.md`, `docs/backend-database-schema/backend_schema.sql` |
| 2026-09-21        | Deploy vs Publish Action Verb                | Primary API endpoint is `POST /v1/capsules/{id}/publish`, directly mirroring the CLI verb `capsule publish`. `POST /v1/capsules/{id}/deploy` is accepted as a direct alias for backwards compatibility with TRD Section 4.                                                                                                        | `docs/TRD.md` Sec 4 & 11, `docs/api-cli-spec/CLI_SPEC.md`, `docs/api-cli-spec/openapi.yaml`                 |
| 2026-09-21        | Version 1 Database Snapshot                  | Initial deployment (Version 1) automatically creates and stores an empty, clean baseline SQLite snapshot (`capsules/{id}/snapshots/{snapshot_id}.sqlite`), satisfying the `db_snapshot_ref NOT NULL` schema constraint and enabling deterministic rollback to baseline.                                                           | `docs/TRD.md` Sec 12 & 13.2, `docs/backend-database-schema/backend_schema.sql`                              |
| 2026-09-21        | Control Plane Technology Stack               | Control Plane API is implemented in Python 3.12+ using FastAPI, Pydantic v2, and SQLAlchemy (asyncpg) per TRD Table 42. CLI and SDK are implemented in TypeScript (Node.js 22). Manifest JSON Schema is shared via `capsule.manifest.schema.json`.                                                                                | `docs/TRD.md` Table 42, `docs/PRD.md` Sec 4.4                                                               |
| 2026-09-21        | Phase 0 Sandbox Driver                       | Implement the abstract `SandboxDriver` interface (Project Rule 9). Use `DockerSandboxDriver` (leveraging Docker Desktop with WSL2 backend) for Phase 0 execution. Provide `DevMockSandboxDriver` strictly for unit tests, clearly documented as NOT a security boundary. Prepares extension for Firecracker/gVisor in production. | `docs/TRD.md` Sec 9, Project Rule 9                                                                         |
| 2026-09-21        | Local Origin & Domain Isolation              | Use `http://<capsule-id>.localhost:8080` for Capsule applications and `http://localhost:8080` for the Edge Proxy/Dashboard. Modern browsers (RFC 6761) treat `*.localhost` subdomains as distinct origins, strictly satisfying Security Invariant 5 without modifying `/etc/hosts`.                                               | `docs/TRD.md` Sec 16 & 55, Security Invariant 5, RFC 6761                                                   |
| 2026-09-21        | Identity / Authentication for Tests          | Edge Proxy enforces standard OIDC JWT verification (JWKS, issuer, audience, expiry) ready for Google/Keycloak. Control Plane includes a built-in `MockOIDCProvider` for local dev and automated tests (`AUTH_PROVIDER=mock`) to satisfy the $<60\text{s}$ offline acceptance test.                                                | `docs/TRD.md` Sec 15 & 27, `docs/PRD.md` Sec 5                                                              |
| 2026-09-21        | SQLite Library for SDK                       | Use `better-sqlite3` as the primary engine inside `@capsule/sdk` with WAL mode enabled. Wrap it in a clean `sdk.getDb()` abstraction with graceful fallback to `node:sqlite` if native compilation is unavailable.                                                                                                                | `docs/TRD.md` Sec 7 & 14                                                                                    |
| 2026-09-21        | Object Storage Driver                        | Implement an abstract `StorageDriver` interface. Use `LocalStorageDriver` (storing files in `.capsule/storage/` under TRD-specified paths) by default for local Phase 0 development, and `S3StorageDriver` for MinIO/AWS S3.                                                                                                      | `docs/TRD.md` Sec 14 & Table 42                                                                             |
| 2026-09-21        | Monorepo Tooling & Package Manager           | Use standard `npm` workspaces for TypeScript/Node.js packages (leveraging pre-installed Node.js 24 and npm 11). Use standard Python virtual environment (`venv` + `pip`) for the Python control plane.                                                                                                                            | `docs/TRD.md` Sec 42                                                                                        |
| 2026-09-21        | Production Sandbox Driver Spike (Prompt 21A) | Comprehensive evaluation of gVisor (`runsc`), Firecracker microVMs, and Managed Providers. **Approved by User: Option A (gVisor `runsc`)** for Phase 1 production sandbox driver behind `SandboxDriver` interface, with Firecracker reserved for Phase 2. Rejects managed providers.                                              | `docs/SANDBOX_DECISION.md`, PRD Security Invariants 1 & 9                                                   |
